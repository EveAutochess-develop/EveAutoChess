# EVE asteroid belt ore meshes

Imported from Tranquility:

`res:/dx9/model/celestial/asteroid/rock_*`

via `tools/import_eve_asteroids.py` (GR2 → OBJ → Assimp GLB).

Used by `AsteroidBelt` under the board midline (22 scatter + `MiningAnchor` per rock).
