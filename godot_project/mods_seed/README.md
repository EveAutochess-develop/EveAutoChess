# mods_seed

随包种子目录：`ModManager` 开机若 `mods_root` 尚无同名包则整树拷贝并默认 `enabled: false`。

狈头级浮游炮台样例已迁出 → [`../mod_samples/beitou-float-turret`](../mod_samples/beitou-float-turret)；由 `publish_disv1.ps1` 打成 `H:\disv1\beitou-float-turret.zip`，不进 PCK。
