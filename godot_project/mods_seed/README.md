# mods_seed

随包种子目录：`ModManager` 开机若 `mods_root` 尚无同名包则整树拷贝并默认 `enabled: false`。

| 包 | 说明 |
|----|------|
| `beitou-float-turret/` | 编辑器验证用样例（狈头级浮游炮台）；与 `mod_samples/beitou-float-turret` 同源 |

导入路径（手动）：`res://mod_samples/beitou-float-turret` 或本目录。

权威：`docs/MOD_PROTOCOL.md` · 仓4 `MODS.md`。
